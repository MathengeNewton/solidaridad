export interface HeroSectionCardsPropsType {
    backgroundImage: string;
    title: string;
    buttonText: string;
};

export interface ProductProps{
    imageSrc: string;
    productName: string;
    productPrice: string;
}
  